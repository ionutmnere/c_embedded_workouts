# include <stdlib. h>
# include <stdio.h>

double compute_add (double num_1, double num_2)
{
    return num 1 + num_2;
}
double compute_subtract (double num_1, double num_2)
{
    return num 1 - num_2;
}
int main()
{
    char first_name [] = "Harry"
    char last_name [] = "Potter"
    double result_add = compute_add(first_name, last_name);
    double result_subtract = compute_subtract(first_name, last_name);
    return EXIT_SUCCESS;
}